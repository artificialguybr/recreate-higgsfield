# Server-only Higgsfield credentials; never prefix these with VITE_.
# Create keys at https://console.higgsfield.ai
# Without them, the app uses catalog previews and demo results.
HF_API_KEY_ID=
HF_API_KEY_SECRET=
